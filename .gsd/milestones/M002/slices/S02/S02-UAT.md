# S02: API Route Migration — UAT

**Milestone:** M002
**Written:** 2026-03-25T14:15:37.193Z

# S02 UAT: API Route Migration\n\n## Preconditions\n- Neon database provisioned with schema and seed data (S01 complete)\n- DATABASE_URL configured in .env.local\n- Dev server running (`bun dev`)\n\n## Test 1: Hero Availability GET\n1. `curl http://localhost:3000/api/hero-availability?teamId=1`\n2. **Expected:** JSON array of objects with `heroId` and `isAvailable` fields for all 5 classes in team 1\n3. **Expected:** HTTP 200 status\n\n## Test 2: Hero Availability POST\n1. `curl -X POST http://localhost:3000/api/hero-availability -H 'Content-Type: application/json' -d '{\"heroId\": 1, \"isAvailable\": false}'`\n2. **Expected:** JSON with updated availability record\n3. **Expected:** HTTP 200 status\n\n## Test 3: Team Members GET\n1. `curl http://localhost:3000/api/team-members?teamId=1`\n2. **Expected:** JSON with `members` array (may be empty if no registrations yet)\n3. **Expected:** HTTP 200 status\n\n## Test 4: Team Invite POST (Generate Code)\n1. `curl -X POST http://localhost:3000/api/team-invite -H 'Content-Type: application/json' -d '{\"teamId\": 1}'`\n2. **Expected:** JSON with invite object containing `code` (nanoid), `teamId`, `teamName`\n3. **Expected:** HTTP 200 status\n4. Save the returned `code` for Test 5\n\n## Test 5: Team Invite GET (Lookup)\n1. `curl http://localhost:3000/api/team-invite?code=<code-from-test-4>`\n2. **Expected:** JSON with invite details including team name from JOIN query\n3. **Expected:** HTTP 200 status\n\n## Test 6: Team Invite Check GET\n1. `curl http://localhost:3000/api/team-invite/check?teamId=1`\n2. **Expected:** JSON with most recent invite for team 1, or empty object if none\n3. **Expected:** HTTP 200 status\n\n## Test 7: Register POST\n1. ```\ncurl -X POST http://localhost:3000/api/register \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"fullName\": \"Test User\", \"email\": \"test@example.com\", \"phone\": \"0123456789\", \"age\": 25, \"gender\": \"male\", \"teamId\": 1, \"heroId\": 1, \"lineNumber\": 1, \"cgLeader\": \"Leader A\", \"shirtSize\": \"M\", \"dietaryRestrictions\": \"none\"}'\n```\n2. **Expected:** HTTP 200 with registration confirmation\n3. **Expected:** Pre-check errors return appropriate HTTP 4xx codes (e.g., duplicate email → 409)\n\n## Test 8: Health Check\n1. `curl http://localhost:3000/api/health-check`\n2. **Expected:** JSON with `status: 'healthy'` from Neon DB\n3. **Expected:** HTTP 200 status\n\n## Test 9: No Supabase Imports (Static Check)\n1. Run `rg 'from.*@/lib/supabase' src/app/api/`\n2. **Expected:** Zero matches (exit code 1)\n\n## Test 10: TypeScript Compilation\n1. Run `npx tsc --noEmit 2>&1 | grep -E 'hero-availability|team-members|team-invite|register/route|health-check'`\n2. **Expected:** Zero type errors in any API route file\n\n## Edge Cases\n- Register with duplicate email → should return conflict error\n- Register with taken line number → should return conflict error\n- Team invite lookup with invalid code → should return 404\n- Hero availability for non-existent team → should return empty array or 404\n- Missing required fields in register POST → should return 400 validation error"
